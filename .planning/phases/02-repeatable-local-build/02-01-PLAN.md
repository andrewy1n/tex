---
phase: 02-repeatable-local-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .latexmkrc
  - Makefile
autonomous: true
requirements:
  - BLD-01
must_haves:
  truths:
    - "Repo contains a committed `.latexmkrc` so `latexmk` behavior is reproducible without ad-hoc flags"
    - "From repo root, `latexmk` (or `make pdf`) completes with exit 0 and produces `main.pdf` when TeX is installed"
  artifacts:
    - path: .latexmkrc
      provides: "Project latexmk configuration (D-13)"
      contains: "pdf_mode|pdflatex"
    - path: Makefile
      provides: "Optional `make pdf` wrapper (D-14)"
      contains: "latexmk"
  key_links:
    - from: Makefile
      to: main.tex
      via: "latexmk builds main.tex at repo root"
---

<objective>
Add a committed **`.latexmkrc`** and a thin **`Makefile`** so the same **`latexmk`** invocation CONTEXT locked in **D-11** is reproducible from a fresh clone, and verify **`main.pdf`** is produced from **`main.tex`** at the repository root.

Purpose: Satisfies the **mechanism** side of **BLD-01** (command exists and works); README documentation is **02-02-PLAN.md**.
Output: `.latexmkrc`, `Makefile` at repo root.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-repeatable-local-build/02-CONTEXT.md
@.planning/phases/02-repeatable-local-build/02-RESEARCH.md
@main.tex

**Locked decisions:** D-11 (canonical `latexmk` line), D-13 (commit `.latexmkrc`), D-14 (Makefile only as thin wrapper to same invocation), D-16 (`pdflatex` default).

**Out of scope:** CI, devcontainers, xelatex/lualatex as default.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `.latexmkrc` for pdflatex + nonstop builds</name>
  <read_first>
    - .planning/phases/02-repeatable-local-build/02-CONTEXT.md
    - main.tex
  </read_first>
  <files>.latexmkrc</files>
  <action>
    Create `.latexmkrc` at the repository root (Perl syntax for `latexmk`).

    Must encode:
    - PDF output via **`pdflatex`** (e.g. `$pdf_mode = 1;` or equivalent supported by the project’s `latexmk` version).
    - Non-interactive builds: pass **`-interaction=nonstopmode`** and **`-file-line-error`** into the `pdflatex` invocation (CONTEXT D-11 style).

    Include a one-line comment at the top that this file is the project `latexmk` config referenced from README / Makefile.

    Do not add package-specific custom dependencies unless required for a minimal `article` build (current `main.tex` has no `\\bibliography` yet).
  </action>
  <verify>
    <automated>test -f .latexmkrc && test -s .latexmkrc && grep -qE 'pdf_mode|pdflatex' .latexmkrc</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .latexmkrc` exits 0
    - `grep -qE 'pdf_mode|\\$pdf_mode' .latexmkrc || grep -q pdflatex .latexmkrc`
    - `grep -qi nonstopmode .latexmkrc || grep -qi interaction .latexmkrc`
    - `grep -q file-line-error .latexmkrc || grep -q file.line.error .latexmkrc`
  </acceptance_criteria>
  <done>`.latexmkrc` exists and encodes pdflatex PDF build with nonstop + file-line-error behavior</done>
</task>

<task type="auto">
  <name>Task 2: Add thin `Makefile` (`make pdf` → same latexmk as CONTEXT)</name>
  <read_first>
    - .planning/phases/02-repeatable-local-build/02-CONTEXT.md
    - .latexmkrc
  </read_first>
  <files>Makefile</files>
  <action>
    Create a **`Makefile`** at repo root with:

    - A **`.PHONY: pdf`** declaration.
    - A **`pdf`** target whose recipe runs **exactly** this command (CONTEXT D-11), using a **TAB** for the recipe line:
      `latexmk -pdf -interaction=nonstopmode -file-line-error main.tex`

    Optional: a **`clean`** target running `latexmk -C` (only if it does not introduce a second divergent build path).

    Do not add compiler rules that bypass `latexmk` for the primary PDF path.
  </action>
  <verify>
    <automated>test -f Makefile && grep -q 'latexmk' Makefile && grep -q 'main.tex' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `test -f Makefile` exits 0
    - `grep -q '^pdf:' Makefile || grep -q '^pdf :' Makefile`
    - `grep -q 'latexmk -pdf' Makefile`
    - `grep -q 'main.tex' Makefile`
  </acceptance_criteria>
  <done>`Makefile` wraps the canonical latexmk line for `main.tex`</done>
</task>

<task type="auto">
  <name>Task 3: Verify build from repo root</name>
  <read_first>
    - Makefile
    - main.tex
    - .latexmkrc
  </read_first>
  <files>(none — verification only)</files>
  <action>
    From the **repository root**, run:

    `latexmk -pdf -interaction=nonstopmode -file-line-error main.tex`

    Expect **exit code 0**. Confirm **`main.pdf`** exists next to **`main.tex`** after the run.

    If TeX is not installed in the execution environment, document the failure reason in SUMMARY.md under Issues and still ensure config files are correct — do not fake `main.pdf` in git (it is gitignored).
  </action>
  <verify>
    <automated>test -f main.pdf && test main.pdf -nt main.tex 2>/dev/null || test -f main.pdf</automated>
  </verify>
  <acceptance_criteria>
    - `latexmk -pdf -interaction=nonstopmode -file-line-error main.tex` exits 0 **OR** SUMMARY.md records TeX missing with manual verification note
    - If exit 0: `test -f main.pdf` exits 0
  </acceptance_criteria>
  <done>Build verified or documented blocker for environments without TeX</done>
</task>

</tasks>

<verification>
  - `latexmk` and `make pdf` (if Make installed) both target `main.tex` consistently with CONTEXT.
</verification>

<success_criteria>
  - `.latexmkrc` and `Makefile` committed; `main.pdf` producible from repo root when TeX is present.
</success_criteria>

<output>
After completion, create `.planning/phases/02-repeatable-local-build/02-repeatable-local-build-01-SUMMARY.md`
</output>
