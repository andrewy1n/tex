---
phase: 01-repository-layout-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - REPO-03
must_haves:
  truths:
    - "LaTeX auxiliary and log-like patterns from a standard TeX .gitignore are present"
    - "Build-output PDF at repo root is ignored; figure PDFs under figures/ can remain trackable via negation rules"
  artifacts:
    - path: .gitignore
      provides: "TeX-oriented ignore list (D-09, D-04)"
      contains: "*.aux"
  key_links:
    - from: .gitignore
      to: figures/
      via: "Optional !figures/**/*.pdf negation (D-05) so intentional figure PDFs can be tracked"
---

<objective>
Add a comprehensive TeX `.gitignore` at the repo root so auxiliary and log files are excluded and default build PDF output is ignored (D-04, D-09); include narrow negation patterns so PDFs under `figures/` may be tracked as sources (D-05).

Purpose: Satisfies the ignore-file portion of REPO-03 and aligns with research guidance not to hand-roll a minimal list (D-10).
Output: Root `.gitignore` based on GitHub’s `TeX.gitignore` plus project-specific exceptions documented in plan 01-03 README.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-repository-layout-documentation/01-CONTEXT.md
@.planning/REQUIREMENTS.md

**Locked decisions:** D-04 (ignore aux + built PDF next to `main.tex`), D-05 (figure PDFs in `figures/` may be tracked), D-09 (baseline from GitHub `TeX.gitignore`), D-10 (do not invent a minimal list from scratch).

**Reference URL (informative):** https://raw.githubusercontent.com/github/gitignore/main/TeX.gitignore

**Note:** README “what we track vs ignore” text is implemented in `01-03-PLAN.md` (same requirement REPO-03).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor GitHub TeX.gitignore baseline</name>
  <read_first>
    - .planning/phases/01-repository-layout-documentation/01-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>.gitignore</files>
  <action>
    Fetch the current content of GitHub’s `TeX.gitignore` from `https://raw.githubusercontent.com/github/gitignore/main/TeX.gitignore` and write it to the repository root as `.gitignore`. If the fetch fails, retry with `curl -fsSL` or document failure and paste the same pattern categories from a local mirror—preference is live GitHub raw content (D-09).

    Append a short comment section at the end, e.g. `# --- project overrides (tex workspace) ---`, before overrides.
  </action>
  <verify>
    <automated>test -f .gitignore && test -s .gitignore && rg -q '\.aux|\.log' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .gitignore` exits 0
    - `wc -c .gitignore` is greater than 200 bytes (baseline TeX list is substantial, not a one-line stub)
    - `rg -n '\\.aux|\\.log|\\.out|synctex' .gitignore` matches at least two distinct auxiliary pattern lines
  </acceptance_criteria>
  <done>`.gitignore` exists and contains standard TeX auxiliary patterns per D-09</done>
</task>

<task type="auto">
  <name>Task 2: Project overrides for PDF output and figures</name>
  <read_first>
    - .gitignore
    - .planning/phases/01-repository-layout-documentation/01-CONTEXT.md
  </read_first>
  <files>.gitignore</files>
  <action>
    Per D-04: ensure the **built** PDF at repo root is ignored. If the upstream `TeX.gitignore` already ignores `*.pdf` globally, add **after** the bulk patterns:

    - `!figures/**/*.pdf` (or equivalent) so intentional figure PDFs under `figures/` remain trackable (D-05).

    If upstream does **not** ignore all PDFs, still add an explicit rule for the known output name `main.pdf` at root (since master file is `main.tex`), e.g. `/main.pdf`, so build output is never committed by default.

    Keep comments in `.gitignore` explaining: (1) root `main.pdf` is build output; (2) `figures/**/*.pdf` may be source assets. Plan 01-03 README will restate this for humans (D-06).
  </action>
  <verify>
    <automated>rg -q 'main\.pdf|figures' .gitignore || rg -q '\*\.pdf' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '^/main\\.pdf|main\\.pdf' .gitignore` matches OR `rg -n '\\*\\.pdf' .gitignore` matches (root build PDF policy explicit)
    - `rg -n '!figures|figures/\*\*/\*\\.pdf' .gitignore` matches (negation for figure PDFs when global pdf ignore exists); if no global `*.pdf` rule, negation may be omitted—then `rg -n 'main\\.pdf' .gitignore` must match for `/main.pdf` or `main.pdf`
  </acceptance_criteria>
  <done>Build PDF ignored; figure PDF exception documented in comments and patterns per D-04–D-05</done>
</task>

</tasks>

<verification>
  - `git check-ignore -v main.pdf` returns a matching rule when a dummy `main.pdf` exists (optional local test), OR pattern audit shows `/main.pdf` or equivalent.
</verification>

<success_criteria>
  - `.gitignore` is TeX-comprehensive (not hand-rolled minimal).
  - Default build output at root is ignored; `figures/` PDF negation present if `*.pdf` is globally ignored.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-layout-documentation/01-repository-layout-documentation-02-SUMMARY.md`
</output>
