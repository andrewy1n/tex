---
phase: 02-repeatable-local-build
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - README.md
  - main.tex
autonomous: true
requirements:
  - BLD-01
must_haves:
  truths:
    - "README documents exactly one primary command to build the PDF, working directory, and prerequisites"
    - "README includes the same canonical latexmk invocation as CONTEXT D-11 (or points unambiguously to Makefile that uses it)"
  artifacts:
    - path: README.md
      provides: "Build instructions (D-15)"
      contains: "latexmk"
    - path: main.tex
      provides: "No longer claims build is undocumented"
  key_links:
    - from: README.md
      to: Makefile
      via: "Documents make pdf as optional alias"
    - from: README.md
      to: main.tex
      via: "Names main.tex as entry; build from root"
---

<objective>
Update **`README.md`** with a **Build** section that documents the **exact** primary command(s), **repository root** as working directory, and prerequisites—fulfilling ROADMAP success criteria and **BLD-01** documentation. Tweak **`main.tex`** so it no longer says the build is only “future Phase 2” now that instructions exist.

Purpose: Human/agent discoverability; Phase 3 will reference this section.
Output: Revised `README.md`, minor `main.tex` wording.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-repeatable-local-build/02-CONTEXT.md
@README.md
@Makefile
@.latexmkrc
@main.tex

**Depends on:** Plan **02-01** (`.latexmkrc` + `Makefile` + verified latexmk line) so README examples match real files.

**Locked:** D-15 (README-only docs surface), D-12 (cwd = repo root), D-11 (canonical command text).
</context>

<tasks>

<task type="auto">
  <name>Task 1: README — Build section with primary command and cwd</name>
  <read_first>
    - README.md
    - Makefile
    - .planning/phases/02-repeatable-local-build/02-CONTEXT.md
  </read_first>
  <files>README.md</files>
  <action>
    Add a **`## Build`** (or **`## Compile`**) section to `README.md` that includes:

    1. **Prerequisite:** full TeX distribution on PATH with **`latexmk`** available (reuse existing TeX Live link from Prerequisites if present).

    2. **Working directory:** state explicitly: run commands from the **repository root** (directory containing `main.tex`).

    3. **Primary command (BLD-01):** document **`latexmk -pdf -interaction=nonstopmode -file-line-error main.tex`** as the canonical one-shot build (must match CONTEXT D-11 and the `Makefile` `pdf` target).

    4. **Optional:** one line that **`make pdf`** runs the same recipe if `make` is available (points to `Makefile`).

    5. **Output:** `main.pdf` next to `main.tex` (ignored by `.gitignore` — already explained elsewhere in README).

    Remove **all** forward-reference lines that defer the build to Phase 2, including:
    - Prerequisites: the sentence fragment about “Phase 2 will document … `latexmk`” (integrate into Build or drop once Build exists).
    - **Main file:** replace “The exact build command will be documented in Phase 2.” with a pointer to **`## Build`**.
    - **What we track vs ignore:** remove “The build command will be documented in Phase 2.” at the end.

    Update the **Repository layout** fenced tree to include root **`Makefile`** and **`.latexmkrc`** next to `main.tex`.

    Keep sections coherent; avoid duplicating long prerequisite prose.
  </action>
  <verify>
    <automated>grep -q '## Build\|## Compile' README.md && grep -q 'latexmk -pdf' README.md && grep -qi 'root\|repository root' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^## (Build|Compile)' README.md`
    - `grep -q 'latexmk -pdf -interaction=nonstopmode -file-line-error main.tex' README.md`
    - `grep -qi 'repository root\|repo root' README.md`
    - `! grep -q 'Phase 2' README.md` (no remaining “Phase 2” build deferrals)
    - `grep -q 'Makefile' README.md` and `grep -q '.latexmkrc' README.md` (layout tree lists both)
  </acceptance_criteria>
  <done>README contains explicit build command, cwd, and prereqs</done>
</task>

<task type="auto">
  <name>Task 2: main.tex — align body text with documented build</name>
  <read_first>
    - README.md
    - main.tex
  </read_first>
  <files>main.tex</files>
  <action>
    Edit `main.tex` body so it no longer claims the build command is only documented in a future phase. Replace the placeholder sentence with a short pointer to the README **Build** section (e.g. “Build: see README”) or a neutral statement that the build is documented in README.

    Preserve `\\documentclass{article}` and overall structure.
  </action>
  <verify>
    <automated>grep -q 'README\|build' main.tex && ! grep -q 'Phase~2' main.tex</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi README main.tex`
    - `! grep -q 'Phase~2' main.tex`
    - `grep -q '\\documentclass' main.tex`
  </acceptance_criteria>
  <done>Master file text matches “build documented in README” reality</done>
</task>

</tasks>

<verification>
  - A new reader can copy-paste the documented `latexmk` line from README and succeed from repo root with TeX installed.
</verification>

<success_criteria>
  - BLD-01 satisfied: one documented primary command + cwd + clone-friendly instructions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-repeatable-local-build/02-repeatable-local-build-02-SUMMARY.md`
</output>
