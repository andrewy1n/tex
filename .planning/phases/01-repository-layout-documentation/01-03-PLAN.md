---
phase: 01-repository-layout-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - README.md
autonomous: true
requirements:
  - REPO-01
  - REPO-02
  - REPO-03
  - REPO-04
must_haves:
  truths:
    - "README names main.tex, lists prerequisites, and shows directory layout"
    - "README explains tracked vs ignored artifacts (PDFs, aux files) without reading .gitignore line-by-line"
  artifacts:
    - path: README.md
      provides: "Onboarding and policy (D-07, D-08, D-06)"
      contains: "main.tex"
  key_links:
    - from: README.md
      to: main.tex
      via: "Explicit statement that main.tex is the compile root"
    - from: README.md
      to: .gitignore
      via: "What we track vs ignore summarizes .gitignore intent"
---

<objective>
Write a short root `README.md` so a new reader can find the compile root, understand folder layout, prerequisites, and git/artifact policy (REPO-01, REPO-02, REPO-03 documentation clause, REPO-04), following D-06–D-08.

Purpose: Completes Phase 1 onboarding without implementing build commands (Phase 2).
Output: `README.md` at repository root.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repository-layout-documentation/01-CONTEXT.md
@main.tex
@.gitignore

**Locked decisions:** D-06 (README “what we track vs ignore”), D-07 (short README: purpose, main file, tree, one-line prereq, link to TeX Live or distro-neutral install docs), D-08 (optional one-line LaTeX Workshop note).

**Depends on:** Plan 01-01 (`main.tex`, directories) and 01-02 (`.gitignore`) so paths and policy match reality.
</context>

<tasks>

<task type="auto">
  <name>Task 1: README structure and compile root / prerequisites</name>
  <read_first>
    - main.tex
    - .gitignore
    - .planning/phases/01-repository-layout-documentation/01-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>README.md</files>
  <action>
    Create or replace root `README.md` with:

    1. **Title + one-line purpose** aligned with PROJECT.md (LaTeX sources in git, editor/agent friendly).

    2. **Prerequisites (REPO-04 / D-07):** State that a full TeX distribution is required (e.g. TeX Live or MacTeX or MiKTeX—distro-neutral wording) and mention `latexmk` for future Phase 2 workflow. Include a markdown link to `https://www.tug.org/texlive/` (or a small “install TeX” section with that link).

    3. **Main / compile root (REPO-01, REPO-04):** Explicit sentence: the primary LaTeX entry file is `main.tex` at the repository root (D-01). Note that Phase 2 will document the exact build command; do not add Makefile/latexmk instructions beyond a forward-looking one-liner if needed.

    4. **Optional (D-08):** One line that VS Code/Cursor users may install LaTeX Workshop (`James-Yu.latex-workshop`)—no settings dump.
  </action>
  <verify>
    <automated>test -f README.md && rg -q 'main\.tex' README.md && rg -qi 'tex live|miktex|mactex|latexmk' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'main\\.tex' README.md` has at least one match in body text (not only inside a code block is fine)
    - `rg -ni 'prerequisite|requirement|install|TeX' README.md` matches (prerequisites section or equivalent)
    - `rg -n 'tug\\.org/texlive|texlive' README.md` matches (install link per D-07)
  </acceptance_criteria>
  <done>README states main file and prerequisites with TeX Live link</done>
</task>

<task type="auto">
  <name>Task 2: Layout summary and artifact policy</name>
  <read_first>
    - README.md
    - .gitignore
    - .planning/phases/01-repository-layout-documentation/01-CONTEXT.md
  </read_first>
  <files>README.md</files>
  <action>
    Add to `README.md`:

    1. **Directory layout (REPO-02, REPO-04):** A fenced markdown code block or ASCII tree showing at least: `main.tex`, `figures/`, `bib/` (with `references.bib`), `sections/`, `.gitignore`, `README.md`. Match the actual repo after plan 01-01.

    2. **What we track vs ignore (REPO-03, D-06):** Short bullet list: tracked — `.tex`, `.bib`, figure sources (e.g. `.png`, `.eps`, PDFs under `figures/` when they are source assets); ignored — LaTeX aux files (`.aux`, `.log`, `.out`, `.synctex.gz`, etc.) and **root** `main.pdf` build output. State that details live in `.gitignore` (GitHub TeX baseline + project overrides).

    Do not claim a build command is already documented unless Phase 2 is done; phrase as “build command will be documented in Phase 2” or similar if needed.
  </action>
  <verify>
    <automated>rg -q 'figures|bib|sections' README.md && rg -qi 'track|ignore|aux|\.gitignore' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'figures|bib|sections' README.md` matches all three path names
    - `rg -ni 'ignore|aux|synctex|main\\.pdf|pdf' README.md` matches (artifact policy present)
    - `rg -n '\\.gitignore' README.md` matches (pointer to ignore file)
  </acceptance_criteria>
  <done>REPO-02–REPO-04 documentation complete in README; artifact policy explicit per D-06</done>
</task>

</tasks>

<verification>
  - A reader can answer in under one minute: What file is the master? Where do figures and bibliography go? What must be installed? What is ignored?
</verification>

<success_criteria>
  - README satisfies REPO-01 (names compile root), REPO-02 (describes layout), REPO-03 (documents track/ignore policy), REPO-04 (prerequisites + main file + layout summary).
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-layout-documentation/01-repository-layout-documentation-03-SUMMARY.md`
</output>
