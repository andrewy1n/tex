---
phase: 03-agent-edit-conventions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
autonomous: true
requirements:
  - AGT-01
must_haves:
  truths:
    - "Repository root contains AGENTS.md naming main.tex as master and pointing to README Build without duplicating latexmk blocks"
    - "AGENTS.md states minimal automation expectations: no aux commits, preserve % continuations, no root main.pdf commits"
  artifacts:
    - path: AGENTS.md
      provides: "Human+agent convention contract (D-17, D-19, D-20)"
      contains: "main.tex"
    - path: AGENTS.md
      provides: "Build by reference to README"
      contains: "README.md"
---

<objective>
Create **`AGENTS.md`** at the repository root: the **primary** convention document for humans and coding agents—**master/compile root**, **build instructions by reference only** to `README.md` **## Build** (no pasted `latexmk`/`make` blocks per **D-19**), and **roadmap-minimum** automated-edit expectations per **D-20**.

Purpose: Satisfies **AGT-01** document body and ROADMAP success criteria items 1–3 for the dedicated convention file.
Output: `AGENTS.md` only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agent-edit-conventions/03-CONTEXT.md
@README.md
@.gitignore
@main.tex

**Locked:** D-17 (`AGENTS.md` primary), D-19 (pointer to README Build only), D-20 (minimal edit expectations).

**Out of scope:** Changing README Build text, `.cursor/rules`, Makefile, `.latexmkrc`, or expanding beyond minimal expectations.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add root AGENTS.md</name>
  <read_first>
    - .planning/phases/03-agent-edit-conventions/03-CONTEXT.md
    - README.md
    - main.tex
    - .gitignore
  </read_first>
  <files>AGENTS.md</files>
  <action>
    Create **`AGENTS.md`** at the repository root with these sections (headings may vary slightly; content must cover every bullet):

    1. **Title** — e.g. `# Agent and edit conventions` (or equivalent).

    2. **Master / compile root** — State explicitly that the primary entry document is **`main.tex`** at the **repository root** (same directory as `README.md`).

    3. **Build** — **Do not** paste the full `latexmk` or `make` command blocks. Instead, instruct readers to follow **`README.md`**, section **`## Build`** (include the exact heading text `## Build` and the filename `README.md`). You may add a markdown link such as `[README.md — Build](README.md#build)` if the renderer supports anchors.

    4. **Git and automated edits** — State **all** of the following in plain language:
       - Do **not** commit LaTeX **auxiliary** or **log** files (e.g. `.aux`, `.log`, `.out`, `.synctex.gz`) or other generated noise covered by `.gitignore`.
       - **Preserve** TeX **`%` line continuations** where they are used (do not strip continuation newlines in ways that merge lines incorrectly).
       - Do **not** commit the **root** **`main.pdf`** next to `main.tex` (build output; ignored by project policy).

    5. **Optional one-liner** — Point to **`.gitignore`** for the authoritative ignore patterns (no need to list every extension).

    Keep the document **short** (roughly one screen); no duplicate of README Build code fences.
  </action>
  <verify>
    <automated>test -f AGENTS.md && grep -q 'main.tex' AGENTS.md && grep -q 'README.md' AGENTS.md && grep -q '## Build' AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f AGENTS.md` exits 0
    - `grep -q 'main.tex' AGENTS.md`
    - `grep -q 'README.md' AGENTS.md`
    - `grep -q '## Build' AGENTS.md`
    - `! grep -q 'latexmk -pdf' AGENTS.md` (no duplicated primary build command line from README)
    - `grep -qiE 'aux|\\.log|synctex|gitignore' AGENTS.md`
    - `grep -q '%' AGENTS.md`
    - `grep -q 'main.pdf' AGENTS.md`
  </acceptance_criteria>
  <done>AGENTS.md exists with master file, README Build pointer, and minimal git/edit expectations</done>
</task>

</tasks>

## PLANNING COMPLETE
