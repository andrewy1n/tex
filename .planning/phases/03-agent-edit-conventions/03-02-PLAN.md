---
phase: 03-agent-edit-conventions
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - README.md
  - .cursor/rules
autonomous: true
requirements:
  - AGT-01
must_haves:
  truths:
    - "README visibly points readers to AGENTS.md for agent/automation conventions"
    - "Cursor rules include a short pointer to AGENTS.md without duplicating LaTeX policy prose"
  artifacts:
    - path: README.md
      provides: "Pointer to AGENTS.md (D-17)"
      contains: "AGENTS.md"
    - path: .cursor/rules
      provides: "Pointer to AGENTS.md (D-18)"
      contains: "AGENTS.md"
---

<objective>
Wire **`AGENTS.md`** into discoverability: add a **short pointer** in **`README.md`** (per **D-17**) and a **minimal LaTeX/agent pointer** in **`.cursor/rules`** (per **D-18**)—without duplicating long convention text in either file.

Purpose: Completes **AGT-01** onboarding path and CONTEXT integration points; assumes **`AGENTS.md`** from plan **03-01** exists.
Output: Updated `README.md`, updated `.cursor/rules`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agent-edit-conventions/03-CONTEXT.md
@AGENTS.md
@README.md
@.cursor/rules

**Depends on:** Plan **03-01** (`AGENTS.md` must exist).

**Locked:** D-17 (README pointer), D-18 (rules pointer only).
</context>

<tasks>

<task type="auto">
  <name>Task 1: README — pointer to AGENTS.md + layout tree</name>
  <read_first>
    - README.md
    - AGENTS.md
    - .planning/phases/03-agent-edit-conventions/03-CONTEXT.md
  </read_first>
  <files>README.md</files>
  <action>
    Update **`README.md`**:

    1. Add a **short** subsection (suggested: **`## Agents`** or **`## AI assistants`**) placed **after** the top intro paragraph and **before** **`## Prerequisites`** OR immediately **after** **`## Build`** — choose one location that reads naturally; it must be visible without scrolling past the entire README on a typical laptop.

    The subsection body must be **1–3 sentences** and include a markdown link or explicit filename reference to **`AGENTS.md`** stating that it defines conventions for **coding agents** / **automated edits** (wording flexible).

    2. Update the **Repository layout** fenced tree to include **`AGENTS.md`** at the repository root (same level as `main.tex`, `README.md`).

    Do **not** paste the full contents of `AGENTS.md` into README.
  </action>
  <verify>
    <automated>grep -q 'AGENTS.md' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'AGENTS.md' README.md`
    - `grep -qE '^## (Agents|AI assistants|For agents)' README.md`
    - `grep -q 'AGENTS.md' README.md` appears in the layout code block section (e.g. `grep -A20 'Repository layout' README.md | grep -q 'AGENTS.md'`)
  </acceptance_criteria>
  <done>README links to AGENTS.md and lists it in the layout tree</done>
</task>

<task type="auto">
  <name>Task 2: .cursor/rules — short pointer to AGENTS.md</name>
  <read_first>
    - .cursor/rules
    - AGENTS.md
    - .planning/phases/03-agent-edit-conventions/03-CONTEXT.md
  </read_first>
  <files>.cursor/rules</files>
  <action>
    Edit **`.cursor/rules`** to add a **minimal** pointer to **`AGENTS.md`** for LaTeX-specific agent editing expectations.

    Requirements:
    - **Do not** remove or rewrite existing GSD/stack/profile blocks.
    - Insert **one short HTML comment-delimited block** or **2–4 lines** immediately **after** the `<!-- gsd-conventions-end -->` line (before **`<!-- gsd-architecture-start`** is ideal) **or** as the first lines inside the **Conventions** section if you must keep file structure — preference: new small block after `gsd-conventions-end`.

    The inserted text must:
    - Mention **`AGENTS.md`** at repo root.
    - State that LaTeX source edit expectations live there (one sentence).

    Do **not** paste the full `AGENTS.md` body into `.cursor/rules`.
  </action>
  <verify>
    <automated>grep -q 'AGENTS.md' .cursor/rules</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'AGENTS.md' .cursor/rules`
    - `grep -q 'gsd-project-start' .cursor/rules` (ensure GSD project block still present)
    - `grep -q 'gsd-workflow-start' .cursor/rules` (ensure GSD workflow block still present)
  </acceptance_criteria>
  <done>.cursor/rules points to AGENTS.md without removing existing GSD content</done>
</task>

</tasks>

## PLANNING COMPLETE
