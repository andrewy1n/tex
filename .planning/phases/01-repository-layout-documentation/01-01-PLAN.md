---
phase: 01-repository-layout-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.tex
  - figures/.gitkeep
  - bib/references.bib
  - sections/.gitkeep
autonomous: true
requirements:
  - REPO-01
  - REPO-02
must_haves:
  truths:
    - "Repository root contains a single obvious compile root file named main.tex"
    - "figures/, bib/, and sections/ exist for assets and split sources per documented layout"
  artifacts:
    - path: main.tex
      provides: "Primary LaTeX entry document at repo root (D-01)"
      contains: "\\documentclass"
    - path: figures/.gitkeep
      provides: "Tracked placeholder so figures/ exists in clone"
    - path: bib/references.bib
      provides: "Bibliography input location (D-02: bib/)"
    - path: sections/.gitkeep
      provides: "Optional partials directory for future \\input/\\include"
  key_links:
    - from: main.tex
      to: bib/references.bib
      via: "Future \\bibliography or biblatex path; README names bib/ in plan 01-03"
---

<objective>
Create the repository’s physical layout and a minimal root `main.tex` so the compile root is unambiguous without reading the README (per REPO-01 / D-01) and sources, figures, and bibliography inputs have predictable homes (REPO-02 / D-02, D-03).

Purpose: Establishes the tree Phase 2 build and Phase 3 `AGENTS.md` will assume.
Output: `main.tex` at repo root, `figures/`, `bib/references.bib`, `sections/` placeholder.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repository-layout-documentation/01-CONTEXT.md

**Locked decisions:** D-01 (`main.tex` at repo root), D-02 (`figures/`, `bib/` with bibliography files; use `bib/references.bib`), D-03 (one document per repo, no `papers/<name>/`).

**Out of scope for this phase:** Makefile / `latexmk` recipe (Phase 2), `AGENTS.md` (Phase 3).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add standard directories and bibliography stub</name>
  <read_first>
    - .planning/phases/01-repository-layout-documentation/01-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>figures/.gitkeep, bib/references.bib, sections/.gitkeep</files>
  <action>
    Create `figures/` with a tracked `.gitkeep` (empty figures directory must still clone cleanly).

    Create `bib/references.bib` with a single comment line explaining it is the project bibliography (e.g. `% Project bibliography — add entries here.`). Do not add a full BibTeX entry unless needed for a minimal compile; Phase 1 does not require a successful PDF build.

    Create `sections/` with `.gitkeep` for optional `\\input`/`\\include` partials (D-02). README will describe this layout in plan 01-03.
  </action>
  <verify>
    <automated>test -f figures/.gitkeep && test -f bib/references.bib && test -f sections/.gitkeep && test -d figures && test -d bib && test -d sections</automated>
  </verify>
  <acceptance_criteria>
    - `test -f figures/.gitkeep` exits 0
    - `test -f bib/references.bib` exits 0
    - `test -f sections/.gitkeep` exits 0
    - `rg -n "references\.bib|bibliography" bib/references.bib` optional; file is non-empty or contains comment only
  </acceptance_criteria>
  <done>figures/, bib/, sections/ exist with tracked placeholders; `bib/references.bib` present per D-02</done>
</task>

<task type="auto">
  <name>Task 2: Add root main.tex compile root</name>
  <read_first>
    - .planning/phases/01-repository-layout-documentation/01-CONTEXT.md
    - figures/.gitkeep
    - bib/references.bib
  </read_first>
  <files>main.tex</files>
  <action>
    Add `main.tex` at the repository root (D-01). Use `\\documentclass{article}` and a minimal body (e.g. title or placeholder paragraph) so the file is valid LaTeX. Do not add `\\bibliography`/`\\addbibresource` unless you also ensure a clean single-pass compile; a minimal article without citations is acceptable for Phase 1.

    First line or nearby comment may state that this file is the project master/root for editors and future build docs.

    Do not add Makefile, `.latexmkrc`, or build scripts (Phase 2).
  </action>
  <verify>
    <automated>test -f main.tex && rg -q '\\documentclass' main.tex</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ./main.tex` exits 0
    - `rg -n '^[^%]*\\\\documentclass' main.tex` matches at least one line
    - `rg -n 'main\\.tex|master|root' main.tex` OR file lives at repo root as `./main.tex` (satisfies obvious compile root per REPO-01)
  </acceptance_criteria>
  <done>REPO-01 satisfied via single obvious root `main.tex`; REPO-02 layout dirs exist for figures, bib, sections</done>
</task>

</tasks>

<verification>
  - From repo root: `ls main.tex figures bib sections` shows expected paths.
  - No Phase 2 build artifacts committed.
</verification>

<success_criteria>
  - Obvious compile root `main.tex` exists at repository root.
  - `figures/`, `bib/` (with `references.bib`), and `sections/` exist per D-02.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-layout-documentation/01-repository-layout-documentation-01-SUMMARY.md`
</output>
