---
phase: 04-multi-project-layout-index
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - PROJECTS.md
  - README.md
  - AGENTS.md
autonomous: true
requirements:
  - PRJ-03
  - PRJ-04
must_haves:
  truths:
    - "`PROJECTS.md` lists the sample project with master path and a copy-paste `latexmk` line from repo root"
    - "`README.md` describes multi-project layout and links to `PROJECTS.md`"
    - "`AGENTS.md` explains multiple compile roots under `projects/` and points to README/PROJECTS for commands without duplicating full `latexmk` blocks"
  artifacts:
    - path: PROJECTS.md
      provides: "Project index (D-29)"
      contains: "projects/sample"
    - path: README.md
      provides: "Multi-project overview + pointer (D-30)"
      contains: "PROJECTS.md"
    - path: AGENTS.md
      provides: "Agent multi-project policy (D-31, D-32)"
      contains: "projects/"
---

<objective>
Add **`PROJECTS.md`** as the canonical project index (**D-29**), update **`README.md`** for multi-project layout and navigation (**D-30**), and update **`AGENTS.md`** for multiple compile roots and per-project artifact policy (**D-31–D-32**) without duplicating full build command blocks.

Purpose: Satisfies **PRJ-03** and the documentation half of **PRJ-04**.
Output: `PROJECTS.md`, updated `README.md`, updated `AGENTS.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-multi-project-layout-index/04-CONTEXT.md
@.planning/phases/03-agent-edit-conventions/03-CONTEXT.md
@README.md
@AGENTS.md
@Makefile

**Depends on:** Plan **04-01** completed (migrated paths and Makefile target exist).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `PROJECTS.md` project index table</name>
  <read_first>
    - .planning/phases/04-multi-project-layout-index/04-CONTEXT.md
    - Makefile
  </read_first>
  <files>PROJECTS.md</files>
  <action>
    Create **`PROJECTS.md`** at repository root.

    Include a **table** (or equivalent list) with at least these columns per **D-29**:
    - **Project name** (e.g. `sample`)
    - **Master file path** (e.g. `projects/sample/main.tex`)
    - **Build from repo root** — copy-paste line exactly:
      `latexmk -pdf -interaction=nonstopmode -file-line-error projects/sample/main.tex`

    Add a one-line intro stating this file is the **canonical** list of projects in the repo.

    Optional (discretion): note **`make pdf`** if **`Makefile`** still provides a thin wrapper to the same invocation.
  </action>
  <verify>
    <automated>test -f PROJECTS.md && grep -q 'projects/sample/main.tex' PROJECTS.md && grep -q 'latexmk' PROJECTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f PROJECTS.md` exits 0
    - `grep -q 'projects/sample/main.tex' PROJECTS.md`
    - `grep -q 'latexmk -pdf' PROJECTS.md`
  </acceptance_criteria>
  <done>`PROJECTS.md` exists with master path and build command for `sample`</done>
</task>

<task type="auto">
  <name>Task 2: Update `README.md` for multi-project layout and navigation</name>
  <read_first>
    - README.md
    - PROJECTS.md
    - .planning/phases/04-multi-project-layout-index/04-CONTEXT.md
  </read_first>
  <files>README.md</files>
  <action>
    Update **`README.md`**:

    1. **Repository layout** — ASCII tree must show **`projects/`** (at least **`projects/sample/`** with **`main.tex`**, **`bib/`**, **`figures/`**, **`sections/`**) and **no** root-level **`main.tex`**.

    2. **Multi-project** — Short explanation that each subfolder of **`projects/`** is an independent LaTeX project with its own **`main.tex`**.

    3. **Build** — Per **D-30**, either:
       - Keep **`## Build`** with the **exact** canonical command for **`projects/sample/main.tex`** from repo root, **or**
       - State that per-project commands live in **`PROJECTS.md`** and include **one** explicit example line for **`sample`** plus a link to **`PROJECTS.md`**.

    4. **Main file** section — Remove “`main.tex` at repository root”; point to **`PROJECTS.md`** and **`projects/sample/main.tex`** as the primary example.

    5. **What we track vs ignore** — Mention per-project **`main.pdf`** under **`projects/`** is ignored like root **`main.pdf`** was.

    Preserve **Prerequisites** and **Agents** pointer style from Phase 3.
  </action>
  <verify>
    <automated>grep -q 'projects/' README.md && grep -q 'PROJECTS.md' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'projects/sample' README.md || grep -q 'projects/' README.md`
    - `grep -q 'PROJECTS.md' README.md`
    - `grep -q '## Build' README.md`
    - README does not use the old single-line claim that the only master is `main.tex` at repo root (spot-check: no phrase like "primary LaTeX entry is **\`main.tex\`** at the repository root" unchanged from pre-v1.1)
  </acceptance_criteria>
  <done>README reflects multi-project layout and links to PROJECTS.md; Build section matches CONTEXT</done>
</task>

<task type="auto">
  <name>Task 3: Update `AGENTS.md` for multi-project compile roots and git hygiene</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/03-agent-edit-conventions/03-CONTEXT.md
    - .planning/phases/04-multi-project-layout-index/04-CONTEXT.md
  </read_first>
  <files>AGENTS.md</files>
  <action>
    Update **`AGENTS.md`**:

    1. **Master / compile root** — Replace single-root wording with: each project’s master is **`projects/<name>/main.tex`**; **`sample`** is **`projects/sample/main.tex`**.

    2. **Build** — Keep **by-reference only** (**D-19** / **D-31**): point to **`README.md`** **`## Build`** and **`PROJECTS.md`** for per-project commands. **Do not** paste multi-line `latexmk` blocks.

    3. **Git / automated edits** — Extend “do not commit PDF” to **per-project** built PDFs: e.g. do not commit **`projects/**/main.pdf`**; aux patterns unchanged.

    4. Optionally one line: coding agents should discover the active project via **`PROJECTS.md`** or user context.

    Keep **`%` line continuation** and **`.gitignore`** pointer bullets.
  </action>
  <verify>
    <automated>grep -q 'projects/' AGENTS.md && grep -q 'PROJECTS.md\|README.md' AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'projects/sample/main.tex\|projects/<name>/main.tex\|projects/' AGENTS.md`
    - `grep -qi 'PROJECTS.md\|README' AGENTS.md`
    - `grep -qi 'main.pdf' AGENTS.md`
  </acceptance_criteria>
  <done>`AGENTS.md` describes multi-project roots and pointers; no duplicate latexmk blocks</done>
</task>

</tasks>

<verification>
  - Human spot-check: open **`PROJECTS.md`**, **`README.md`**, **`AGENTS.md`** — consistent paths and no stale root-`main.tex` master claim.
</verification>

<success_criteria>
  - PRJ-03 and documentation aspects of PRJ-04 satisfied by committed markdown files.
</success_criteria>

## PLANNING COMPLETE
