# Quick Task 260327-heb: Add Synctex to latexmk pdflatex build

**Mode:** quick

## Tasks

1. **Build** — Commit `.latexmkrc` change: add `-synctex=1` to the `pdflatex` invocation so PDF ↔ source sync works with the LaTeX Workshop / editor.
2. **GSD artifacts** — Write `260327-heb-SUMMARY.md`; append row to `STATE.md` Quick Tasks Completed; final `docs(quick-260327-heb)` commit for planning files.

**Scope:** Only the root `.latexmkrc` Synctex change — do not include unrelated working tree changes (e.g. submodule pointer bumps).
