---
phase: 04-multi-project-layout-index
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/sample/main.tex
  - projects/sample/figures/
  - projects/sample/bib/
  - projects/sample/sections/
  - .gitignore
  - Makefile
autonomous: true
requirements:
  - PRJ-01
  - PRJ-02
  - PRJ-04
must_haves:
  truths:
    - "`projects/sample/main.tex` exists and is the only migrated master; no `main.tex` remains at repo root"
    - "`projects/sample/main.pdf` is gitignored via a `projects/**/main.pdf` (or equivalent) rule"
    - "`make pdf` and documented `latexmk` use explicit path `projects/sample/main.tex` from repo root"
  artifacts:
    - path: projects/sample/main.tex
      provides: "Migrated compile root (D-23)"
      contains: "documentclass"
    - path: Makefile
      provides: "Thin latexmk wrapper for sample project (D-27)"
      contains: "projects/sample/main.tex"
    - path: .gitignore
      provides: "Per-project PDF ignore + figure negation (D-26)"
      contains: "projects"
---

<objective>
Create **`projects/sample/`**, **move** the existing root LaTeX tree into it per **D-23–D-24**, update **`.gitignore`** for per-project PDFs and figure exceptions per **D-26**, and align the **`Makefile`** with **D-27** so builds do not clobber other projects’ outputs.

Purpose: Satisfies **PRJ-01** (convention exists on disk), **PRJ-02** (isolated PDF per project path), and the **`.gitignore`** portion of **PRJ-04**.
Output: `projects/sample/**`, updated `.gitignore`, updated `Makefile`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-multi-project-layout-index/04-CONTEXT.md
@.planning/phases/04-multi-project-layout-index/04-RESEARCH.md
@.planning/REQUIREMENTS.md
@README.md
@Makefile
@.gitignore
@main.tex

**Locked:** D-21–D-28. **Out of scope:** Phase 5 editor settings; `PROJECTS.md`/README prose (plan **04-02**).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `projects/sample/` and move root LaTeX sources</name>
  <read_first>
    - .planning/phases/04-multi-project-layout-index/04-CONTEXT.md
    - main.tex
    - README.md
  </read_first>
  <files>projects/sample/main.tex; projects/sample/figures/; projects/sample/bib/; projects/sample/sections/</files>
  <action>
    Create directory **`projects/sample/`** at repo root.

    Using **`git mv`** (preserve history), move from repository root into **`projects/sample/`**:
    - **`main.tex`**
    - **`figures/`** (entire directory)
    - **`bib/`** (entire directory)
    - **`sections/`** (entire directory — include `.gitkeep` if present)

    After the move: **no** **`main.tex`** at repo root.

    Update **`projects/sample/main.tex`** body text minimally if needed so the **Build** reference remains correct: it must still point readers to **`README.md`** **`## Build`** (or **`PROJECTS.md`** if README defers there — prefer still mentioning README per Phase 3 single source for command policy). At minimum, ensure the sentence does not claim the master lives at repo root.

    Do **not** add new LaTeX packages or bibliography wiring unless required for paths to resolve.
  </action>
  <verify>
    <automated>test ! -f main.tex && test -f projects/sample/main.tex && test -d projects/sample/bib && test -d projects/sample/figures</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f main.tex` exits 0
    - `test -f projects/sample/main.tex` exits 0
    - `test -d projects/sample/bib` exits 0
    - `test -d projects/sample/figures` exits 0
    - `git status` shows moves (not deletions + adds) for tracked files where `git mv` was used
  </acceptance_criteria>
  <done>Root sample tree lives under `projects/sample/` with no root `main.tex`</done>
</task>

<task type="auto">
  <name>Task 2: Generalize `.gitignore` for per-project PDFs and figure PDFs</name>
  <read_first>
    - .planning/phases/04-multi-project-layout-index/04-CONTEXT.md
    - .gitignore
  </read_first>
  <files>.gitignore</files>
  <action>
    In **`.gitignore`**, under the project overrides section (after the GitHub TeX baseline):

    1. Keep existing root rule **`/main.pdf`** (covers stale or accidental root builds per D-32 “not only” wording).

    2. Add ignore for built project PDFs: e.g. **`projects/**/main.pdf`** (or equivalent glob that matches each project’s colocated output per **D-25–D-26**).

    3. Add negation so intentional figure PDFs under project trees remain trackable: e.g. **`!projects/**/figures/**/*.pdf`** (mirror prior **`!figures/**/*.pdf`** intent).

    4. Optionally retain **`!figures/**/*.pdf`** if a root-level **`figures/`** could reappear; if removed, document in a one-line comment why **`projects/**`** covers tracked figure PDFs.

    Include short comments tying rules to **D-26** / per-project policy so agents understand intent.
  </action>
  <verify>
    <automated>grep -q "projects" .gitignore && grep -q "main.pdf" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE 'projects/\*\*/main\.pdf|projects/\*\*/\*\.pdf' .gitignore || grep -q 'projects' .gitignore && grep -q 'main.pdf' .gitignore`
    - `grep -q 'projects/\*\*/figures' .gitignore || grep -q '!projects' .gitignore`
  </acceptance_criteria>
  <done>`.gitignore` ignores per-project `main.pdf` and preserves figure-PDF exceptions under `projects/`</done>
</task>

<task type="auto">
  <name>Task 3: Point `Makefile` at `projects/sample/main.tex` and verify build</name>
  <read_first>
    - Makefile
    - .latexmkrc
    - .planning/phases/04-multi-project-layout-index/04-CONTEXT.md
  </read_first>
  <files>Makefile</files>
  <action>
    Update **`Makefile`** so the **`pdf`** target runs **`latexmk`** with the **exact flags** already used today (**`-pdf -interaction=nonstopmode -file-line-error`**) but targeting **`projects/sample/main.tex`** instead of **`main.tex`** (per **D-27**).

    Update **`clean`** (if present) to run **`latexmk -C`** against **`projects/sample/main.tex`** so aux cleanup targets the migrated master.

    From repo root, run:
    `latexmk -pdf -interaction=nonstopmode -file-line-error projects/sample/main.tex`

    Expect exit **0** when TeX is installed; confirm **`projects/sample/main.pdf`** exists after build. If TeX is missing in CI/agent env, record in execution **SUMMARY.md** and still verify Makefile/grep correctness.
  </action>
  <verify>
    <automated>grep -q 'projects/sample/main.tex' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'projects/sample/main.tex' Makefile`
    - `grep -q 'latexmk' Makefile`
    - `latexmk -pdf -interaction=nonstopmode -file-line-error projects/sample/main.tex` exits 0 **implies** `test -f projects/sample/main.pdf`
  </acceptance_criteria>
  <done>`make pdf` equivalent path documented in Makefile; sample project builds to `projects/sample/main.pdf` when TeX is available</done>
</task>

</tasks>

<verification>
  - No second project required for PRJ-02; isolation is demonstrated by distinct path `projects/<name>/main.pdf` and ignore rules.
</verification>

<success_criteria>
  - Migrated tree under `projects/sample/`; `.gitignore` and `Makefile` match CONTEXT D-23–D-28.
</success_criteria>

## PLANNING COMPLETE
